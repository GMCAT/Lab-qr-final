function profileDate(value) { return value ? new Date(value).toISOString().slice(0,10) : ''; }
function renderProfileView(app, user) {
  app.innerHTML = `<div class="min-h-screen bg-slate-50 px-4 py-8"><main class="mx-auto max-w-2xl rounded-3xl border bg-white p-6 shadow-sm sm:p-8"><div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">โปรไฟล์ของฉัน</h1><p class="text-sm text-slate-500">แก้ไขข้อมูลส่วนตัวของบัญชี</p></div><a href="${isAdmin() ? '#/admin' : '#/home'}" class="rounded-xl border px-4 py-2 text-sm font-semibold">กลับ</a></div><form id="profileForm" class="mt-6 grid gap-4 sm:grid-cols-2"><label class="sm:col-span-2 text-sm font-semibold">ชื่อ-นามสกุล<input name="name" required maxlength="200" value="${escapeHtml(user.name || '')}" class="mt-1 w-full rounded-xl border px-4 py-3"></label><label class="text-sm font-semibold">อีเมล<input disabled value="${escapeHtml(user.email || '')}" class="mt-1 w-full rounded-xl border bg-slate-100 px-4 py-3"></label><label class="text-sm font-semibold">ตำแหน่ง<input name="position" maxlength="200" value="${escapeHtml(user.position || '')}" class="mt-1 w-full rounded-xl border px-4 py-3"></label><label class="text-sm font-semibold">แผนก / ห้อง Lab<input name="department_lab" maxlength="200" value="${escapeHtml(user.department_lab || '')}" class="mt-1 w-full rounded-xl border px-4 py-3"></label><label class="text-sm font-semibold">เบอร์โทร<input name="phone" maxlength="50" value="${escapeHtml(user.phone || '')}" class="mt-1 w-full rounded-xl border px-4 py-3"></label><label class="text-sm font-semibold">วันเกิด<input name="birth_date" type="date" value="${profileDate(user.birth_date)}" class="mt-1 w-full rounded-xl border px-4 py-3"></label><button id="profileSubmit" class="rounded-xl bg-teal-700 px-4 py-3 font-semibold text-white sm:col-span-2">บันทึกโปรไฟล์</button></form></main></div>`;
  document.getElementById('profileForm').onsubmit = async event => { event.preventDefault(); const button = document.getElementById('profileSubmit'); button.disabled = true; try { const result = await api('/auth/profile', { method:'PUT', body:JSON.stringify(Object.fromEntries(new FormData(event.target))) }); localStorage.setItem('user', JSON.stringify(result.user)); appToast(result.message); renderProfileView(app, result.user); } catch (error) { appToast(error.message, 'error'); button.disabled = false; } };
}
window.renderProfileView = renderProfileView;
